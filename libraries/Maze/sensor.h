#ifndef __SENSOR_H__
#define __SENSOR_H__

/* virtual sensor inputs to get sensor call.
 * 3x3 maze with each cell's wall configuration
 * stored as 2D array.
 */
uint8_t _maze[] = {
		9,10,12,
		5,13,5,
		7,3,6
};

uint8_t _maze1[] = {
		9,10,14,
		1,10,12,
		7,11,6
};

#endif